# Archived services

DORMANT or superseded implementations. Not used in production.

- **OrderProcessingService** — Order write path is OrderEngine + Core RPCs (`create_order_atomic`, `process_order_payment`). See `core-engine/ARCHITECTURE_DECISION.md`.
