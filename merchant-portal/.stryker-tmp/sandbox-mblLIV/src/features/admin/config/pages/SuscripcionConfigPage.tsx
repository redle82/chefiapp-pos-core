/**
 * SuscripcionConfigPage — Assinatura / Billing Center dentro de Configuración.
 * Igual ao Last.app: Configuración > Suscripción = plano, uso, cobrança, pagamento, faturas.
 */

import { SubscriptionPage } from "../../subscription/pages/SubscriptionPage";

export function SuscripcionConfigPage() {
  return <SubscriptionPage />;
}
