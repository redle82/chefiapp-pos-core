# AUDITORIA SUPREMA - Bypass Hunt Allowlist
# One regex pattern per line. Empty lines and lines starting with # are ignored.
# Use only for intentional/justified exceptions.

# BootstrapPage.tsx — kernel-level tenant write (setActiveTenant) and boot-time tab reads.
# These are the ONLY legitimate writers that seed the tenant into sessionStorage
# before TenantContext is initialized. Converting to useTenant() would create a
# circular dependency (TenantContext reads what BootstrapPage writes).
BootstrapPage\.tsx.*setActiveTenant
BootstrapPage\.tsx.*getTabIsolated

# AuthProvider.tsx — kernel-level Sofia default tenant seeding.
# Sets a default tenant for dev/demo mode before the context tree mounts.
AuthProvider\.tsx.*(setActiveTenant|readTenantIdWithLegacyFallback)

# CookieConsentBanner.tsx — renders at App root, OUTSIDE TenantProvider.
# Uses getTabIsolated for best-effort tenant ID in legal consent audit trail.
# Cannot use useTenant() because TenantProvider is only in AppOperationalWrapper.
CookieConsentBanner\.tsx.*getTabIsolated
