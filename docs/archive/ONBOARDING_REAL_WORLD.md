# Real World Onboarding: Pilot Protocol (Phase J.3)

**Objective:** Onboard the first real-world user (Pilot) into the ChefIApp Production Environment.

## 1. Environment Readiness (The Checklist)

Before sharing the link, ensure the **Vercel Project Settings** has these Environment Variables configured:

| Variable | Value (Example/Source) |
| :--- | :--- |
| `VITE_API_BASE` | `https://7614de306f3ac8.lhr.life` (SSH Tunnel Active) |
| `VITE_SUPABASE_URL` | `https://xyz.supabase.co` (Your Project Settings) |
| `VITE_SUPABASE_ANON_KEY` | `eyJ...` (Your Project Settings) |
| `VITE_STRIPE_PUBLIC_KEY` | `pk_test_...` (or `pk_live_...` for Real Money) |

> **⚠️ CRITICAL:** After setting these variables, you must **Redeploy** (or "Promote") the latest deployment in Vercel for them to take effect.

## 2. The Pilot Link

**Share this URL with the User:**
> https://merchant-portal-oie0hpun8-goldmonkeys-projects.vercel.app

*(Note: If you have configured a custom domain like `app.chefiapp.com`, use that instead).*

## 3. The Onboarding Flow (Script)

Walk the user through these steps (or execute them yourself as a proxy):

1.  **Landing:** User lands on the Welcome Page.
2.  **Identity:** Enter "Restaurant Name" (e.g., "Cantinho do Prego").
3.  **Menu Setup:** Add 3-5 items to define the menu structure.
4.  **Payment (The Gate):** 
    *   User sees the "ChefIApp Pro" plan (€29/mo).
    *   Click "Subscribe".
    *   Enter Card Details (Use **Test Card** `4242...` if using Test Keys).
5.  **Activation:**
    *   Upon success, the user is redirected to the `Dashboard`.
    *   **Verify:** "TPV" and "Orders" buttons are unlocked.

## 4. Verification (God Mode)

Monitor the process from your terminal:

1.  **Watch the Server Log:**
    ```bash
    # You should see incoming requests
    POST /internal/wizard/...
    POST /internal/billing/session
    ```

2.  **Watch Supabase:**
    Check the `restaurants` table. A new entry should appear.
    ```sql
    select * from restaurants order by created_at desc limit 1;
    ```

## 5. Troubleshooting (Emergency)

*   **Tunnel Error:** If the app says "Network Error" or 504, check `npx localtunnel`. It might have crashed or changed URL.
*   **Stripe Error:** Check `VITE_STRIPE_PUBLIC_KEY` matches the backend secret key mode (Test vs Live).
*   **White Screen:** Check Vercel Logs (Function logs or Browser Console).

---
*Generated by Antigravity*
