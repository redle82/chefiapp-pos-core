import * as fs from 'fs';
import * as path from 'path';
import { fileURLToPath } from 'url';
import { createObjectCsvWriter } from 'csv-writer';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// ... rest of the file content ...

const OUTPUT_DIR = path.resolve(__dirname, '../output');

// Ensure output dir exists
fs.mkdirSync(OUTPUT_DIR, { recursive: true });

// ... rest of the file content ...

function generateIssues(testResults, a11yResults) {
  // ... other code ...

  // A11y issues
  a11yResults.forEach((issue) => {
    issues.push({
      id: `A11Y-${Date.now()}-${Math.random().toString(36).slice(2, 11)}`,
      // ... other properties ...
    });
  });

  // Test failure issues
  const errors = (testResults && (testResults.errors || testResults.failures || testResults.suites?.flatMap(s => s.specs || []) || [])) || [];
  if (Array.isArray(errors) && errors.length > 0) {
    errors.forEach((error, index) => {
      // ... handle each error ...
    });
  }

  // ... other code ...
}

node_modules/
output/
button_styles.json
playwright-report/
test-results/
*.log
.DS_Store
