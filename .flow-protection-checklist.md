# 🔒 CHECKLIST DE PROTEÇÃO — FLUXO ARQUITETURAL

**Status:** `E2E_FLOW = LOCKED`  
**Uso:** Code Review obrigatório para mudanças em fluxo de navegação

---

## ⚠️ ANTES DE MERGAR QUALQUER PR QUE TOQUE EM FLUXO

### 🚫 REGRESSÕES ARQUITETURAIS (REJEITAR IMEDIATAMENTE)

- [ ] ❌ Landing Page apontando para `/login` diretamente
- [ ] ❌ Landing Page com query strings (`?oauth=google`, `?mode=migration`)
- [ ] ❌ Landing Page com lógica de "detectar usuário existente"
- [ ] ❌ Múltiplos pontos de decisão de fluxo (fora do FlowGate)
- [ ] ❌ Lógica de redirecionamento fora de `CoreFlow.ts`
- [ ] ❌ Dependência de dados opcionais no FlowGate (`profiles`, `system_config`)

**Se qualquer um desses aparecer → REJEITAR PR + Link para `ARCHITECTURE_FLOW_LOCKED.md`**

---

### ✅ MUDANÇAS PERMITIDAS (COM CAUTELA)

- [ ] ✅ Adicionar novas rotas em `/app/*` (após FlowGate)
- [ ] ✅ Modificar UI do FlowGate (sem mudar lógica)
- [ ] ✅ Adicionar novos estados de onboarding (se necessário)
- [ ] ✅ Melhorar mensagens de erro/log

**Requisito:** Revisar `ARCHITECTURE_FLOW_LOCKED.md` antes de aprovar.

---

### 🔐 NOVOS APPS (Mobile, Staff, Kiosk)

**Regra Universal:**
- [ ] ✅ Todos entram por `/app`
- [ ] ✅ Todos passam pelo FlowGate
- [ ] ✅ Sem exceção

**Se criar app que bypassa FlowGate → REJEITAR PR**

---

## 📋 CHECKLIST DE VALIDAÇÃO

Antes de aprovar PR que toca em fluxo:

1. [ ] Verificar se Landing ainda aponta apenas para `/app`
2. [ ] Verificar se FlowGate ainda é a única autoridade
3. [ ] Verificar se `/app` ainda é portal neutro
4. [ ] Testar fluxo E2E: `/` → `/app` → `/login` → OAuth → Dashboard
5. [ ] Confirmar que não há loops
6. [ ] Confirmar que console mostra decisões do FlowGate

---

## 🧪 TESTE E2E OBRIGATÓRIO

**Cenário:** Aba anônima → Landing → Clica botão

**Fluxo Esperado:**
1. `/` → Landing page renderiza
2. Clica "Entrar em operação" → Navega para `/app`
3. FlowGate intercepta → Detecta `!auth`
4. Redireciona para `/login`
5. Tela de login Google OAuth disponível
6. **Sem loop. Sem confusão.**

**Console Esperado:**
```
[FlowGate] ✅ Allowed: /
[FlowGate] 🛑 Blocked: Auth required -> Go to /login
[FlowGate] ✅ Allowed: /login
```

**Se o fluxo não seguir exatamente isso → REJEITAR PR**

---

## 📚 REFERÊNCIAS

- `ARCHITECTURE_FLOW_LOCKED.md` - Documentação completa
- `merchant-portal/src/core/flow/CoreFlow.ts` - Lógica de decisão
- `merchant-portal/src/core/flow/FlowGate.tsx` - Executor
- `merchant-portal/src/pages/Landing/components/Hero.tsx` - Landing

---

**Última Atualização:** 2026-01-08  
**Mantenedor:** Arquitetura Core

